---
import Components from '../../components/Components.astro';
import Layout from '../../layouts/Layout.astro';
import Breadcrumbs from '../../layouts/Breadcrumbs.astro';
import { fetchPageData } from '../../utils/fetch-page-data';

const data = await fetchPageData('InteriorDesign_Page');

if (!data) {
  return Astro.redirect('/404');
}

const { page, metadata, faqItems } = data;
---

<Layout footerBackground={page.footerBackground} {...metadata} faqItems={faqItems}>
  <Breadcrumbs data={[{ name: page.name, path: page.slug }]} firstItemType={page.firstItemType} />
  <Components data={page.components} />
</Layout>
